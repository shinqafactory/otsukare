  <div class="span3 bs-docs-sidebar subBody">
    <table class="table table-hover table-condensed table-bordered">
	      <tr><td>名前</td><td><%= @user.name %></td></tr>
	      <tr><td>年齢</td><td><%= get_age(@user.age) %></td></tr>
	      <tr><td>性別</td><td><%= get_gender(@user.gender) %></td></tr>
	      <tr><td>職業</td><td><%= @user.occupation %></td></tr>
	      <tr><td>住まい</td><td><%= get_residence(@user.residence) %></td></tr>
	      <tr><td colspan="2">
	      	<%= button_to(
	      		"編集",
	      		{ :controller => 'users', :action => 'user_edit', :id => @user.id },
					  :class => 'btn btn-primary',
					  :method => :get
					) %>
				</td></tr>
    </table>
  </div>

